#=============================================================================
# 生成配置
#=============================================================================
set(appName "pointer")
# 添加可执行文件。
add_executable(${appName} "main.cpp")
